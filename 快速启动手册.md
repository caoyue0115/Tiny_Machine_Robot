# 快速启动手册

这份文档只保留最短可执行步骤，目标是最快完成一次服务端验证或一次实板烧录联调。

## 1. 最快验证服务器

### 1.1 看健康检查

```bash
curl -sS http://<CURRENT_BASE_URL>/healthz
```

### 1.2 用脚本模拟设备端

```bash
cd /home/aitopia/Engineering_Projects/.worktrees/religion-demo-20260407/20260407_宗教大模型云服务器Demo
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
BASE_URL=http://<CURRENT_BASE_URL> python3 scripts/esp_simulator.py /path/to/input.wav --download-audio --output-dir ./tmp
```

在当前我自己这台机器上的测试文件 具体路径示例：

```bash
cd /home/aitopia/Engineering_Projects/.worktrees/religion-demo-20260407/20260407_宗教大模型云服务器Demo
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
BASE_URL=http://<CURRENT_BASE_URL> python3 scripts/esp_simulator.py ./data/output/demo_test_question_24k.wav --download-audio --output-dir ./tmp
```

成功标准：

- `status=done`
- `question_text` 非空
- `audio_url` 非空

如果要验证 `v3/realtime`，直接用：

```bash
cd /home/aitopia/Engineering_Projects/.worktrees/religion-demo-20260407/20260407_宗教大模型云服务器Demo
BASE_URL=http://<CURRENT_BASE_URL> python3 scripts/realtime_smoke.py ./data/output/demo_test_question_24k.wav --max-polls 60
```

如果要模拟板端拿到 `audio_stream_url` 后立刻连接 `/audio`，用：

```bash
BASE_URL=http://<CURRENT_BASE_URL> python3 scripts/realtime_smoke.py ./data/output/demo_test_question_24k.wav --stream-mode immediate --max-polls 60 --audio-timeout 60
```

`immediate` 模式用于判断实时生产速度是否跟得上播放；重点看：

- `production_ratio >= 1`
- `virtual_player.underrun_count == 0`
- `max_inter_chunk_gap_ms` 是否过高

当前 `v3` 远端最好实测大致为：

- LLM 已切到 `qwen3.5-flash-2026-02-23`
- LLM 调用已关闭 thinking：`extra_body.enable_thinking=false`
- LLM prompt 已部署“首句 `6` 到 `10` 字、总回答约 `25` 到 `35` 字”
- `first_audio_byte_ms` 约 `2.1s ~ 2.7s`
- `done_ms` 约 `5.6s ~ 5.9s`

最近一次部署后 live smoke 结果：

- session：`2634b2ab-9a66-403a-984b-07e9c811fd68`
- `first_audio_byte_ms=2628`
- `done_ms=5302`
- `audio_duration_ms=10240`
- 服务端 trace `production_ratio=3.908`
- 脚本拉流 `virtual_player.underrun_count=0`

最近一次 `immediate` live smoke 结果：

- `first_chunk_local_ms` 约 `2.13s ~ 2.81s`
- `production_ratio=2.066 ~ 2.314`（本地拉流视角）
- 服务端 trace `production_ratio=3.812 ~ 3.907`
- `audio_max_chunk_gap_ms=134ms ~ 144ms`
- `virtual_player.underrun_count=0`
- 结论：云端平均生产速度已经明显快于播放速度；实板若仍卡顿，优先看板端 `realtime_audio_summary`

## 2. 最快检查云服务器

如果是硬件同事联调前的云端确认，先做这 4 步。

### 2.1 看公网健康检查

```bash
curl -sS http://<CURRENT_BASE_URL>/healthz
```

正常预期是返回：

- `api=ok`
- `redis=ok`
- `sqlite=ok`
- `asr=ok`
- `llm=ok`
- `tts=ok`

### 2.2 登录云服务器

```bash
ssh -p 22 ubuntu@106.54.240.51
```

### 2.3 看容器是否在线

```bash
docker ps
```

至少应能看到这几个容器处于 `Up`：

- `religion_demo_20260407-api-1`
- `religion_demo_20260407-worker-1`
- `religion_demo_20260407-redis-1`

### 2.4 现场联调时看日志

```bash
docker logs -f religion_demo_20260407-api-1
```

或者：

```bash
docker logs -f religion_demo_20260407-worker-1
```

判断原则：

- `v2` 联调时，API 日志应出现新的 `POST /api/v2/tasks`
- `v2` 后续会出现 `GET /api/v2/tasks/{task_id}`
- `v3` 联调时，API 日志应出现 `POST /api/v3/realtime/sessions`
- `v3` 音频拉流成功时应看到 `GET /api/v3/realtime/sessions/{session_id}/audio`

注意：

- 旧资料中的 `/api/message` 是老接口，不再使用
- 当前联调主链路是 `/api/v2/tasks -> poll -> audio_url`
- 新的低延迟探索线是 `/api/v3/realtime/sessions -> /audio`

## 3. 最快烧录喵伴板子

当前默认基线：

- 开发板：`ESP-VoCat v1.2`
- 核心模组：`ESP32-S3-WROOM-1`
- 工具链：`ESP-IDF 5.5.4`
- 触发方式：`触摸屏`

基础规格速记：

- `ESP32-S3`，`Xtensa` 双核 32 位 `LX7`
- `2.4GHz Wi-Fi (802.11b/g/n)`、`Bluetooth 5`
- `Flash` 最大可选 `16MB`
- 当前板卡确认 `PSRAM=16MB`
- 最多 `36` 个 `GPIO`

### 3.1 先填配置

编辑 [`config.h`](/home/aitopia/Engineering_Projects/.worktrees/religion-demo-20260407/20260407_宗教大模型云服务器Demo/esp_idf_demo/main/config.h#L1)，至少填：

- `DEMO_WIFI_SSID`
- `DEMO_WIFI_PASSWORD`
- `DEMO_SERVER_BASE_URL`
- `DEMO_DEVICE_ID`

推荐先确认：

- `DEMO_SERVER_BASE_URL=http://<CURRENT_BASE_URL>`
- `DEMO_AUDIO_MODE=DEMO_AUDIO_MODE_V3_REALTIME`
- `DEMO_REALTIME_AUDIO_JITTER_BUFFER_BYTES=65536`
- `DEMO_REALTIME_AUDIO_JITTER_PREBUFFER_BYTES=40960`
- `DEMO_REALTIME_INTRO_ENABLED=1`
- `DEMO_REALTIME_INTRO_PATH=/spiffs/intro_1.pcm`
- `DEMO_AUDIO_INPUT_GAIN_DB`
- `DEMO_AUDIO_OUTPUT_VOLUME`
- `DEMO_RECORD_VAD_ENABLED=1`

当前分区与资源：

- app 分区已从默认单 app 扩到 `2MB`
- 新增 `4MB` SPIFFS 分区，标签为 `storage`
- 开场声放在 `esp_idf_demo/spiffs/intro_1.pcm`
- `idf.py flash` 会同时刷入 `build/storage.bin`

当前录音策略：

- 最长录音仍为 `4s`
- 板端轻量 VAD 可提前结束
- 检测到语音后至少录 `1000ms`
- 连续静音 `700ms` 后提前结束
- VAD 是能量阈值方案，不是 WebRTC/Silero 模型
- 串口日志重点看 `vad_stopped`、`voice_started`、`max_level`

### 3.2 构建和烧录

```bash
cd /home/aitopia/Engineering_Projects/.worktrees/religion-demo-20260407/20260407_宗教大模型云服务器Demo/esp_idf_demo
source /home/aitopia/esp/esp-idf-v5.5.4-full/export.sh
idf.py set-target esp32s3
idf.py build
idf.py -p /dev/ttyUSB0 flash monitor
```

## 4. 串口只看这几行

启动时先看：

- `Runtime config`
- `Wi-Fi connected`
- `Initialized ESP-VoCat v1.2 touch trigger`

触摸后再看：

- `Touch trigger event: x=... y=...`
- `stage=record`
- `stage=upload`
- `stage=poll`
- `stage=download`
- `stage=play`
- `pipeline result=ok`

如果接的是 `v3/realtime`，建议额外打印：

- `session_id=...`
- `stage=intro`
- `audio_connect_ms=...`
- `headers_validated=true`
- `first_audio_byte_local_ms=...`
- `playback_start_ms=...`
- `playback_end_ms=...`
- `http_status=...`
- `error_code=...`

## 5. 调参优先级

录音太轻：

- 调高 `DEMO_AUDIO_INPUT_GAIN_DB`

喇叭太小：

- 调高 `DEMO_AUDIO_OUTPUT_VOLUME`

说话经常被截断：

- 先调高 `DEMO_RECORD_VAD_SILENCE_MS`
- 再考虑关闭 `DEMO_RECORD_VAD_ENABLED`
- 最后才调高 `DEMO_RECORD_DURATION_SEC`

等待第一句话偏久：

- 先确认 `vad_stopped=1` 是否生效
- 再看云端 `first_audio_byte_ms`
- 最后再调 `DEMO_REALTIME_AUDIO_JITTER_PREBUFFER_BYTES`

播放仍偶发卡顿：

- 先看 `Realtime jitter playback task done total_in=... total_out=... max_level=...`
- 再看 `realtime_audio_summary ... underrun_count=... max_gap_ms=... prebuffer_wait_ms=...`
- 如果 `underrun_count=0` 但主观仍断续，优先查 TTS 拼接边界或 codec 侧问题，不要继续盲目加 buffer
- 当前默认已调到 `DEMO_REALTIME_AUDIO_JITTER_PREBUFFER_BYTES=40960`，如果仍欠载，应先看云端 `max_gap_ms` 和 TTS 段间供给

## 6. 出问题先怎么判

没有触摸触发：

- 看有没有 `Touch trigger event`

触发后马上失败：

- 看失败发生在哪个 `stage`

上传后一直不完成：

- 看 `Poll attempt=... status=...`

建立了 `/audio` 但迟迟不出声：

- 先看 `first_audio_byte_local_ms`
- 再对照云端 `trace.first_audio_byte_ms`
- 最后查板端 ring buffer 和播放启动点

有下载但没声音：

- 看是否进入 `stage=play`
- 再调 `DEMO_AUDIO_OUTPUT_VOLUME`

## 7. 旧资料处理建议

同级目录 [`20260407_宗教大模型云服务器Demo`](/home/aitopia/Engineering_Projects/20260407_宗教大模型云服务器Demo) 当前看起来只是历史副本：

- 不是 git 仓库
- 只剩 `README.md`、`部署计划大纲.md` 和两份旧快速启动手册

两份旧手册里还有价值的“云端快速检查”和“现场联调配合方式”已经合并进当前这份手册。
如果你确认不再需要保留那份历史副本，就可以删除；如果还想保留早期方案痕迹，就留着当归档。

## 8. v3 联调入口

如果下一步要做板端云端联调，直接看：

- [板端联调清单.md](/home/aitopia/Engineering_Projects/.worktrees/religion-demo-20260407/20260407_宗教大模型云服务器Demo/20260409_流式传输/板端联调清单.md)
- [安装建议.md](/home/aitopia/Engineering_Projects/.worktrees/religion-demo-20260407/20260407_宗教大模型云服务器Demo/20260409_流式传输/安装建议.md)
- [`.env.example`](/home/aitopia/Engineering_Projects/.worktrees/religion-demo-20260407/20260407_宗教大模型云服务器Demo/.env.example)
- 同级联调压缩包：`esp_idf_demo_handoff_2026-04-10.tar.gz`
