-----------

硬件：esp-s3和喵伴的模组https://docs.espressif.com/projects/esp-dev-kits/zh_CN/latest/esp32s3/esp-vocat/index.html
esp开发环境：乐鑫ESP-IDF




Demo优先任务是流程打通
1. ESP32-S3 录音 5 秒 → HTTP POST原始音频流 → Python Flask 服务器接收并保存为 WAV 文件
2. 服务器调用API e.g.对话大模型如来佛人设回答+语音TTS大模型声音克隆
3. 返还音频url

目前想到的待做事项：
1. 云服务器代码部署
2. 对话+语音大模型api调试
3. 写代码模拟esp与云服务器通信


技术方案
Demo先用http+fastAPI
产品用https/websocket


待解决问题：服务器处理需要很长时间
比如调大模型 API 可能要 5-15 秒，HTTP 请求容易超时。可能的解决方案：

# 先立即返回"收到"，然后异步处理
@app.post("/api/message")
def receive_message(msg: Message):
    # 把任务丢进队列后台处理
    process_queue.add(msg)
    return {"status": "accepted", "task_id": generate_id()}

# ESP32 再拿着 task_id 来查结果
@app.get("/api/result/{task_id}")
def get_result(task_id):
    return {"status": "done", "result": "..."}


-----------

# 20260407 追加方案：云端对话 + 云端语音（新云服务器）

## 0. 目标（与现有方案解耦）
1. 不依赖本地模型推理，全部改为云端API（对话/ASR/TTS）。
2. 新开云服务器仅做“业务编排层”（接收音频、调云API、存结果、返回音频）。
3. ESP32-S3 继续保持“录音上传 + 轮询结果”的轻端模式，先打通Demo闭环。
4. 保持与现有 20260404 项目分离，不复用本地 vLLM / 本地RAG 运行链路。

## 1. 范围冻结（Demo）
### 1.1 本期必须做
1. 设备端：5秒录音上传（WAV/PCM），获取 `task_id`。
2. 服务端：异步任务编排（ASR -> 对话LLM -> TTS）。
3. 结果查询：设备轮询 `task_id`，拿到 `audio_url` 后播放。
4. 日志：记录每步耗时与失败原因（ASR/LLM/TTS）。
5. 健康检查：`/healthz` 返回 API 和三方模型连通状态。

### 1.2 本期不做
1. WebSocket 实时流式语音（后续阶段）。
2. 多轮记忆和用户画像。
3. 复杂鉴权（OAuth/JWT多角色）。
4. 多地区多活。

## 2. 总体架构（云端双API）
### 2.1 逻辑链路
1. ESP32 上传音频到 `POST /api/v2/tasks`。
2. 服务端立即返回：`accepted + task_id`（不阻塞等待模型）。
3. 后台Worker异步执行：
- Step A: 云端ASR（语音转文本）
- Step B: 云端对话LLM（佛学约束 + 拒答策略）
- Step C: 云端TTS（返回可下载音频）
4. ESP32轮询 `GET /api/v2/tasks/{task_id}`。
5. 状态 `done` 时获取 `audio_url` 播放。

### 2.2 建议技术栈
1. API：FastAPI + Uvicorn
2. 任务队列：RQ/Celery（二选一，Demo建议 RQ + Redis 更轻）
3. 缓存/队列：Redis
4. 存储：SQLite（Demo）或 PostgreSQL（若你希望云端持久更稳）
5. 文件：本地磁盘 `data/audio/*`（Demo），后续可换 OSS/S3
6. 反向代理：Nginx（HTTPS + 限流 + 静态音频代理）

## 3. 接口契约（建议固定）
### 3.1 `POST /api/v2/tasks`
功能：提交音频任务（multipart/form-data）

入参：
1. `device_id`（string）
2. `audio_file`（wav/pcm）
3. `sample_rate`（int，可选）
4. `format`（string，默认 `wav`）

返回：
```json
{
  "status": "accepted",
  "task_id": "uuid",
  "received_at": "2026-04-07T12:00:00Z"
}
```

### 3.2 `GET /api/v2/tasks/{task_id}`
功能：查询任务状态

返回（处理中）：
```json
{
  "task_id": "uuid",
  "status": "running",
  "step": "tts",
  "progress": 0.8
}
```

返回（完成）：
```json
{
  "task_id": "uuid",
  "status": "done",
  "question_text": "什么是无相",
  "answer_text": "离执着相，见诸法空性。",
  "audio_url": "/api/v2/audio/uuid.wav",
  "latency_ms": 4200,
  "trace": {
    "asr_ms": 900,
    "llm_ms": 1500,
    "tts_ms": 1700
  }
}
```

返回（失败）：
```json
{
  "task_id": "uuid",
  "status": "failed",
  "error_code": "tts_timeout",
  "error_message": "TTS request timeout",
  "retryable": true
}
```

### 3.3 `GET /api/v2/audio/{filename}`
功能：返回音频（`audio/wav` 或 `audio/mpeg`）

### 3.4 `GET /healthz`
建议返回：
```json
{
  "api": "ok",
  "redis": "ok",
  "asr": "ok|down",
  "llm": "ok|down",
  "tts": "ok|down"
}
```

## 4. 核心业务规则（沿用20260404经验）
1. 回答最多30字。
2. 非佛学问题固定拒答：`佛说不可曰`。
3. 对话失败时返回明确系统错误，不伪装成拒答文案。
4. TTS失败时仍返回文字答案，`audio_url=null`，并标注 `tts_status=failed`。
5. 全链路可观测：`request_id/task_id/device_id` 必须贯穿日志。

## 5. 目录建议（新项目）
```text
20260407_宗教大模型云服务器Demo/
  src/
    app.py                 # FastAPI入口
    api/tasks.py           # 提交/查询接口
    workers/pipeline.py    # ASR->LLM->TTS编排
    providers/asr.py       # 云ASR封装
    providers/llm.py       # 云LLM封装
    providers/tts.py       # 云TTS封装
    storage/files.py       # 音频保存/读取
    storage/db.py          # 任务落库
    models/schema.py       # Pydantic模型
    settings.py            # 环境变量配置
  scripts/
    smoke_submit.py        # 模拟ESP上传
    smoke_poll.py          # 轮询与下载
  deploy/
    docker-compose.yml
    nginx.conf
    systemd/
  data/
    incoming/
    output/
    logs/
  docs/
    runbook.md
```

## 6. 环境变量（建议）
```bash
APP_ENV=prod
HOST=0.0.0.0
PORT=8010

REDIS_URL=redis://127.0.0.1:6379/0
SQLITE_PATH=./data/tasks.db

ASR_PROVIDER=dashscope
ASR_API_KEY=
ASR_MODEL=paraformer-realtime-v2

LLM_PROVIDER=dashscope
LLM_API_KEY=
LLM_MODEL=qwen-max-latest
LLM_TEMPERATURE=0.2
LLM_MAX_TOKENS=128

TTS_PROVIDER=dashscope
TTS_API_KEY=
TTS_MODEL=qwen3-tts-vc-2026-01-22
TTS_VOICE=your_voice_id
TTS_TIMEOUT_SECONDS=20

TASK_TIMEOUT_SECONDS=90
MAX_AUDIO_SECONDS=8
MAX_UPLOAD_MB=3
```

## 7. 部署步骤（新云服务器）
### 7.1 服务器准备
1. 新建云服务器（建议与模型API同区域，降低跨洋延迟）。
2. 安装：Docker / Docker Compose / Nginx / Python3.11。
3. 开放端口：`80/443`（公网）、`8010`（仅内网可选）。

### 7.2 应用部署
1. 拉取代码到 `/opt/religion-demo-20260407`。
2. 配置 `.env`（三方API Key）。
3. 启动：
```bash
docker compose up -d --build
```
4. 检查：
```bash
curl http://127.0.0.1:8010/healthz
```

### 7.3 反向代理与HTTPS
1. Nginx 反代到 `127.0.0.1:8010`。
2. 配置证书（Let's Encrypt）。
3. 上传大小限制：
```nginx
client_max_body_size 5m;
proxy_read_timeout 120s;
```

## 8. 延迟治理（重点）
1. 采用“异步任务 + 轮询”，避免ESP HTTP长连接超时。
2. API层只做接收和入队，模型调用放Worker。
3. 三方API复用连接池，避免每次新建连接。
4. 文本先回（可选），音频后到（可选二阶段返回）。
5. 将服务器部署到离模型服务更近的地域（核心收益最大）。

## 9. 可靠性与容错
1. 每个任务最多重试2次（仅重试可恢复错误，如超时）。
2. ASR/LLM/TTS分步超时；单步失败可落库状态。
3. 异常返回统一错误码：
- `asr_timeout`
- `llm_timeout`
- `tts_timeout`
- `provider_auth_failed`
- `invalid_audio_format`
4. 音频文件写入采用临时文件 + 原子替换，防止0字节脏文件。

## 10. 安全基线（Demo最低）
1. API Key 只放服务端，不下发设备端。
2. 上传接口增加简单签名（`device_id + timestamp + token`）。
3. Nginx 限流：按IP/设备ID。
4. 日志脱敏（不打印密钥、完整用户隐私）。

## 11. 验收标准（本期）
1. 设备上传后1秒内拿到 `task_id`。
2. 30次任务中成功率 >= 95%。
3. `GET /tasks/{id}` 状态机完整：`accepted -> running -> done/failed`。
4. 失败任务可读错误码，可复现排障。
5. 有完整链路耗时统计（ASR/LLM/TTS/total）。

## 12. 两阶段执行计划（建议）
### 阶段A：当天打通（MVP）
1. 完成 `/tasks` + `/tasks/{id}` + `/audio/{id}`。
2. 先接固定ASR文本（绕过ASR）验证 LLM+TTS。
3. 再接入真实ASR。
4. 完成ESP模拟脚本。

### 阶段B：次日稳定
1. 接入Redis队列和重试机制。
2. 补齐健康检查与错误码。
3. 做30条压测与日志回放。
4. 输出演示Runbook。

## 13. 关键排障Runbook
1. `healthz` 里 `tts=down`：
- 检查 API Key
- 检查模型名与voice_id是否匹配

2. 任务长期 `running`：
- 检查Worker进程是否在线
- 检查Redis队列积压

3. 返回 `audio_url` 但播放失败：
- 检查文件是否0字节
- 检查 `Content-Type` 与扩展名一致

4. 延迟过高：
- 先看 `trace` 三段耗时
- 识别瓶颈在 ASR / LLM / TTS 哪一段

## 14. 待你确认（请回复）
1. 新云服务器计划放在哪个地域（美国/新加坡/香港）？
2. 云端API是否统一使用阿里云DashScope（ASR+LLM+TTS）？
3. 本期是否需要“文本先回、语音后到”的二阶段响应？
4. ESP端轮询间隔你希望默认多少（建议 700ms）？
5. Demo 是否仍保留“30字以内 + 非佛学拒答”硬规则？


-----------

## 15. 已确认参数（2026-04-07）
1. 云服务器地域：香港。
2. 云端API：统一使用 DashScope（ASR + LLM + TTS）。
3. 技术栈：FastAPI + RQ + Redis + SQLite + 本地磁盘音频存储。
4. ESP轮询间隔：700ms。
5. 回答规则：30字限制可放宽；非佛学拒答可放宽（以链路打通优先）。
6. 新增要求：LLM步骤必须接入 RAG（约20本经书知识库）。
7. Demo阶段安全基线收敛：先不做反向代理与日志脱敏，优先打通链路。

## 16. 决策建议：二阶段 vs 直接返回音频URL
### 16.1 结论（推荐）
推荐保留“二阶段”（`accepted + task_id` -> 轮询拿 `audio_url`），对ESP更稳，不易超时。

### 16.2 原因
1. 云端ASR+LLM+TTS通常存在抖动，单次可能 4-12 秒。
2. ESP与移动网络对长HTTP连接不友好，直返更容易超时/断连。
3. 二阶段可重试、可观测、可追踪，排障成本显著更低。

### 16.3 如果你坚持“单接口直接返 audio_url”
可作为兼容模式增加：
1. `POST /api/v2/tasks?wait_ms=12000`
2. 服务端最多同步等待12秒：
- 若完成：直接返回 `audio_url`
- 未完成：返回 `accepted + task_id`
3. 这样前端体验更直接，同时保留兜底稳定性。

## 17. RAG 方案（20本经书，云端API版）
### 17.1 数据与索引
1. 文档目录：`data/kb/buddhism/`（支持 `.md/.txt/.pdf`）。
2. 预处理：PDF抽取 + 轻清洗（去页眉页脚、断行修复、空行压缩）。
3. 切块建议：`chunk_size=300`，`overlap=50`（可后续A/B）。
4. 检索方案：`BM25 + 向量检索（FAISS）`，本地磁盘持久化。

### 17.2 运行链路
`ASR文本 -> 关键词粗过滤 -> RAG检索TopK -> 证据阈值判断 -> 云端LLM生成 -> TTS`

### 17.3 LLM提示约束（放宽版）
1. 允许回答长度 >30字（建议先放宽到 60-120字用于可懂度）。
2. 非佛学不强制固定拒答，但需优先引导回佛学语境。
3. 仍需返回引用片段（Top 2）提升可信度。

### 17.4 数据结构新增字段
任务结果建议增加：
1. `references`：`[{source_title, snippet, score}]`
2. `decision_type`：`answer|fallback|error`
3. `retrieval_ms`：检索耗时

## 18. Demo最小可交付（按你当前取舍）
### 18.1 Day 1（链路打通）
1. `/tasks` 提交音频 + 立即返回 `task_id`。
2. RQ Worker 串起 ASR -> RAG -> LLM -> TTS。
3. `/tasks/{id}` 返回状态与 `audio_url`。
4. `scripts/smoke_submit.py` + `scripts/smoke_poll.py` 跑通。

### 18.2 Day 2（效果与稳定）
1. 导入20本经书并建索引。
2. 优化检索阈值与TopK。
3. 跑30条任务压测，记录分步耗时（ASR/RAG/LLM/TTS）。
4. 输出一份Demo演示手册（启动、验证、常见故障）。

## 19. 20260407 新窗口执行入口（建议直接照做）
1. 创建项目骨架与 `.env`。
2. 先实现“假ASR文本”打通 RAG->LLM->TTS。
3. 再接真实ASR。
4. 完成ESP模拟与端到端验收。

建议首条启动命令顺序：
```bash
# 终端A
redis-server

# 终端B
uvicorn src.app:app --host 0.0.0.0 --port 8010 --reload

# 终端C
rq worker -u redis://127.0.0.1:6379/0 religion_tasks
```

## 20. 进入开发前最后两项待定
1. DashScope 里 LLM 的具体模型名（例如 `qwen-max-latest` 或你指定版本）是否就定为 `qwen-max-latest`？
2. 20本经书是否都放在同一宗教目录 `buddhism`，暂不做多宗教路由？


-----------

## 21. 最终冻结（已确认）
1. 交互模式：二阶段（`accepted + task_id` -> 轮询结果）。
2. LLM模型：`qwen-max-latest`。
3. 经书目录：统一使用 `buddhism/`（20本经书）。
4. 运行环境：所有实现与经书都在云服务器内。
5. 云服务器：`OLD_PUBLIC_ENTRY_DISABLED_SSH`。

## 22. 新窗口首轮执行清单（按此顺序）
1. SSH登录云服务器，确认代码目录与经书目录存在。
2. 校验 `.env`：
- `LLM_MODEL=qwen-max-latest`
- `ASR_PROVIDER/LLM_PROVIDER/TTS_PROVIDER` 均为 `dashscope`
- `TTS_MODEL` 与 `TTS_VOICE` 配对正确
3. 启动三进程：
- API（FastAPI）
- Worker（RQ）
- Redis
4. 先跑本地 smoke：
- 上传一段样例音频，拿到 `task_id`
- 轮询直到 `done`
- 验证 `audio_url` 可下载播放
5. 再跑ESP模拟脚本，验证端到端闭环。
6. 最后导入20本经书并执行 `ingest`，复测佛学问题检索与回答质量。

## 23. 云端路径建议（统一）
建议在服务器固定：
```text
/opt/religion-demo-20260407/
  src/
  data/
    kb/buddhism/
    incoming/
    output/
  logs/
  .env
```

## 24. 交付口径（给领导演示）
1. 设备录音上传后秒级返回 `task_id`（无阻塞）。
2. 云端异步完成 ASR + RAG + LLM + TTS。
3. 轮询拿结果并播放音频。
4. 支持佛学知识问答（20本经书检索增强）。

## 25. 复用20260404经验（迁移清单）
### 25.1 必须继承
1. RAG默认参数先固定：
- `chunk_size=300`
- `chunk_overlap=50`
- `top_k=4`
- `bm25_weight=0.55`（可后续调参）
2. 双保险策略保留：
- 关键词粗过滤（佛学语义）
- 检索得分阈值裁决（证据不足时保守回答/拒答）
3. 输出结构保留可观测字段：
- `decision_type`
- `tts_status`
- `tts_error`
- `top_score`
- `latency_ms`
4. 音频文件写入策略保留：
- 临时文件写入
- 文件大小校验（非0）
- 原子替换
5. 健康检查保留分项：
- `api`
- `asr`
- `llm`
- `tts`
- `index`（RAG索引）

### 25.2 不要照搬
1. 不迁移本地 vLLM 相关脚本与配置（本期全云端API）。
2. 不迁移浏览器兜底TTS逻辑（服务端链路不需要）。
3. 不迁移“本地GPU资源排障”步骤（转为云API连通排障）。

### 25.3 云端版本新增要点
1. 明确模型-音色配对关系：
- `qwen3-tts-vc-*` 必须配 VC 创建的 `voice_id`
- `qwen3-tts-vd-*` 必须配 VD 创建的 `voice_id`
2. Realtime模型单独链路：
- `qwen3-tts-vc-realtime-*` 需 WebSocket Realtime API，不可直接复用HTTP生成接口。
3. 队列化优先：
- 所有长耗时模型调用放入 RQ Worker，不阻塞主API。

### 25.4 首轮上线前验证（必做）
1. 任务状态机完整：`accepted -> running -> done/failed`。
2. 单条链路日志可追踪：`task_id/request_id/device_id` 一致。
3. `audio_url` 返回的文件可播放且非0字节。
4. 30条压测输出分步耗时：`asr_ms/rag_ms/llm_ms/tts_ms/total_ms`。
