# 使用手册

这份手册用于完成三类事情：

- 验证公网云服务器是否正常
- 在没有硬件时用脚本模拟设备端测试服务器
- 在有喵伴板子时知道该看哪些配置和日志

如果只想最快完成一次验证，优先看：

1. `第 3 节：最快验证路径`
2. `第 4 节：模拟设备测试服务器`
3. `第 7 节：真实喵伴板联调`
4. `第 8 节：常见问题与排查`

## 1. 当前系统链路

当前 Demo 的核心链路是：

`触摸触发 -> 录音 -> 上传 -> 异步处理 -> 轮询 -> 下载返回音频 -> 播放`

分工如下：

- 云服务器：接收原始 PCM、转处理链路、返回 `task_id` 和 `audio_url`
- 设备端：触发、录音、上传、轮询、下载和播放
- 没有硬件时：可用脚本模拟设备端请求

当前还新增了一条并行探索线：

- `v3/realtime`
- 输入仍是整段 PCM 上传
- 输出改成 `HTTP chunked` 裸 `PCM`
- 目标是让板端更早出声

## 2. 关键地址与文件

### 2.1 公网地址

- 公网基准地址：`http://<CURRENT_BASE_URL>`

### 2.2 核心接口

- 提交任务：`POST /api/v2/tasks`
- 查询任务：`GET /api/v2/tasks/{task_id}`
- 下载音频：`GET /api/v2/audio/{filename}`
- 健康检查：`GET /healthz`
- realtime 创建：`POST /api/v3/realtime/sessions`
- realtime 状态：`GET /api/v3/realtime/sessions/{session_id}`
- realtime 音频：`GET /api/v3/realtime/sessions/{session_id}/audio`

### 2.3 关键目录

- 项目根目录：当前目录
- 服务端代码：`src/`
- 测试脚本：`scripts/`
- 设备端工程：`esp_idf_demo/`
- 设备端说明：`esp_idf_demo/README.md`
- 硬件对接说明：`docs/superpowers/summaries/2026-04-08-hardware-handoff.md`
- 快速启动：`快速启动手册.md`
- 板端联调清单：`20260409_流式传输/板端联调清单.md`
- 安装建议：`20260409_流式传输/安装建议.md`
- 环境变量示例：`.env.example`

### 2.4 最常用脚本

- [`esp_simulator.py`](/home/aitopia/Engineering_Projects/.worktrees/religion-demo-20260407/20260407_宗教大模型云服务器Demo/scripts/esp_simulator.py#L1)
- [`smoke_poll.py`](/home/aitopia/Engineering_Projects/.worktrees/religion-demo-20260407/20260407_宗教大模型云服务器Demo/scripts/smoke_poll.py#L1)
- [`realtime_smoke.py`](/home/aitopia/Engineering_Projects/.worktrees/religion-demo-20260407/20260407_宗教大模型云服务器Demo/scripts/realtime_smoke.py#L1)

## 3. 最快验证路径

### 3.1 先看健康检查

```bash
curl -sS http://<CURRENT_BASE_URL>/healthz
```

理想返回类似：

```json
{
  "api": "ok",
  "redis": "ok",
  "asr": "ok",
  "llm": "ok",
  "tts": "ok"
}
```

### 3.2 再用模拟脚本提交一段 WAV

```bash
cd /home/aitopia/Engineering_Projects/.worktrees/religion-demo-20260407/20260407_宗教大模型云服务器Demo
BASE_URL=http://<CURRENT_BASE_URL> python3 scripts/esp_simulator.py /path/to/input.wav --download-audio --output-dir ./tmp
```

### 3.3 成功标准

最少确认：

- `status=done`
- `question_text` 有内容
- `audio_url` 非空
- `downloaded_audio` 指向实际下载到的文件

如果走 `v3/realtime`，最少确认：

- `status=done`
- `trace.first_tts_chunk_ms` 大致在 `3s` 到 `4s`
- `/audio` 返回 `200`
- `audio_headers` 为 `16000/16/1/little`

## 4. 模拟设备测试服务器

这部分适合没有硬件时的联调。

### 4.1 `esp_simulator.py` 做了什么

[`esp_simulator.py`](/home/aitopia/Engineering_Projects/.worktrees/religion-demo-20260407/20260407_宗教大模型云服务器Demo/scripts/esp_simulator.py#L1) 会自动：

1. 读取输入 WAV
2. 提取 PCM 帧
3. 带设备端 Header 上传到 `/api/v2/tasks`
4. 轮询 `/api/v2/tasks/{task_id}`
5. 打印结果
6. 可选下载返回音频

注意：

- 输入给脚本的是 `wav`
- 上传给服务器的是原始 `PCM`

### 4.2 最常用命令

```bash
BASE_URL=http://<CURRENT_BASE_URL> python3 scripts/esp_simulator.py /path/to/input.wav
```

验证 `v3/realtime`：

```bash
BASE_URL=http://<CURRENT_BASE_URL> python3 scripts/realtime_smoke.py ./data/output/demo_test_question_24k.wav --max-polls 60
```

下载返回音频：

```bash
BASE_URL=http://<CURRENT_BASE_URL> python3 scripts/esp_simulator.py /path/to/input.wav --download-audio --output-dir ./tmp
```

### 4.3 生成一条“什么是无相”的测试 WAV

```bash
cd /home/aitopia/Engineering_Projects/.worktrees/religion-demo-20260407/20260407_宗教大模型云服务器Demo
python3 - <<'PY'
from src.providers.tts import synthesize_audio
path, err = synthesize_audio('什么是无相')
print({'path': path, 'error': err})
PY
```

当前已实际生成过一份示例：

- [`demo_test_question_24k.wav`](/home/aitopia/Engineering_Projects/.worktrees/religion-demo-20260407/20260407_宗教大模型云服务器Demo/data/output/demo_test_question_24k.wav)

### 4.4 用这条 WAV 做完整测试

```bash
cd /home/aitopia/Engineering_Projects/.worktrees/religion-demo-20260407/20260407_宗教大模型云服务器Demo
BASE_URL=http://<CURRENT_BASE_URL> python3 scripts/esp_simulator.py ./data/output/demo_test_question_24k.wav --download-audio --output-dir ./tmp
```

这条命令已经实测通过。

### 4.5 常用环境变量

- `BASE_URL`
- `ESP_DEVICE_ID`

常见写法：

```bash
export BASE_URL=http://<CURRENT_BASE_URL>
export ESP_DEVICE_ID=esp-demo-local-test
python3 scripts/esp_simulator.py /path/to/input.wav --download-audio --output-dir ./tmp
```

## 5. 另一种快速 smoke 测法

### 5.1 直接传一段真实 WAV

```bash
BASE_URL=http://<CURRENT_BASE_URL> python3 scripts/smoke_poll.py /path/to/input.wav --expect-success
```

### 5.2 发一段默认静音 PCM

```bash
BASE_URL=http://<CURRENT_BASE_URL> python3 scripts/smoke_poll.py --allow-failure
```

这更适合验证接口是否活着，不适合验证 ASR 效果。

## 6. 如果你要在本地跑服务

### 6.1 安装依赖

```bash
cd /home/aitopia/Engineering_Projects/.worktrees/religion-demo-20260407/20260407_宗教大模型云服务器Demo
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

### 6.2 准备环境变量

至少确认这些值：

- `REDIS_URL`
- `SQLITE_PATH`
- `PUBLIC_BASE_URL`
- `DASHSCOPE_API_KEY`
- `REALTIME_TTS_MODEL`
- `REALTIME_TTS_VOICE`

建议直接参考：

- [`.env.example`](/home/aitopia/Engineering_Projects/.worktrees/religion-demo-20260407/20260407_宗教大模型云服务器Demo/.env.example)

### 6.3 启动服务

```bash
uvicorn src.app:app --host 0.0.0.0 --port 8010
```

### 6.4 本地验证

```bash
curl -sS http://127.0.0.1:8010/healthz
BASE_URL=http://127.0.0.1:8010 python3 scripts/esp_simulator.py /path/to/input.wav
```

本地验证 `v3`：

```bash
BASE_URL=http://127.0.0.1:8010 python3 scripts/realtime_smoke.py ./data/output/demo_test_question_24k.wav --max-polls 60
```

## 7. 真实喵伴板联调

当前设备端工程默认目标已经是：

- 板型：`ESP-VoCat v1.2`
- 触发：`touch`
- 工具链：`ESP-IDF 5.5.4`

### 7.1 板端要对齐的协议

上传到服务器的固定格式：

- 原始 PCM
- `16kHz`
- `16-bit`
- `mono`
- `little-endian`
- `Content-Type: application/octet-stream`

### 7.2 板端必须填写的配置

在 [`config.h`](/home/aitopia/Engineering_Projects/.worktrees/religion-demo-20260407/20260407_宗教大模型云服务器Demo/esp_idf_demo/main/config.h#L1) 里至少要填：

- `DEMO_WIFI_SSID`
- `DEMO_WIFI_PASSWORD`
- `DEMO_SERVER_BASE_URL`
- `DEMO_DEVICE_ID`

### 7.3 最常用调参项

- `DEMO_AUDIO_INPUT_GAIN_DB`
- `DEMO_AUDIO_OUTPUT_VOLUME`
- `DEMO_RECORD_DURATION_SEC`
- `DEMO_CLOUD_POLL_INTERVAL_MS`
- `DEMO_TRIGGER_POLL_INTERVAL_MS`

### 7.4 板端启动命令

```bash
cd esp_idf_demo
source /home/aitopia/esp/esp-idf-v5.5.4-full/export.sh
idf.py set-target esp32s3
idf.py -p /dev/ttyUSB0 flash monitor
```

### 7.5 串口先看什么

启动阶段先看：

- `Runtime config`
- `Wi-Fi connected`
- `Initialized ESP-VoCat v1.2 touch trigger`

触发后再看：

- `Touch trigger event: x=... y=...`
- `stage=record`
- `stage=upload`
- `stage=poll`
- `stage=download`
- `stage=play`
- `pipeline result=ok`

如果切到 `v3/realtime`，额外建议打印：

- `session_id`
- `audio_connect_ms`
- `first_audio_byte_local_ms`
- `playback_start_ms`
- `playback_end_ms`
- `http_status`
- `error_code`

## 8. 常见问题与排查

### 8.1 `curl /healthz` 不通

- 先查服务是否在跑
- 再查公网和端口

### 8.2 模拟脚本缺依赖

优先执行：

```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
```

### 8.3 提交成功但轮询一直不结束

- 看 worker 是否正常
- 看 Redis 是否正常
- 看后端 Provider 是否卡住

### 8.4 `status=failed`

优先看：

- `error_code`
- `error_message`
- `trace`

### 8.5 模拟脚本能通，但实板不通

优先排查：

- Wi-Fi 是否真的联网
- 上传体是否是原始 PCM
- Header 是否带齐
- 录音增益是否过低
- 触摸是否真的触发了 pipeline

### 8.6 下载成功但没有声音

- 先调高 `DEMO_AUDIO_OUTPUT_VOLUME`
- 看是否进入了 `stage=play`
- 看 `audio_out` 的 WAV 解析日志

### 8.7 `v3` 已连上但首播很慢

- 先看云端 `trace.first_audio_byte_ms`
- 再看板端 `first_audio_byte_local_ms`
- 如果两者差距明显，优先排查：
  - chunk 接收策略
  - ring buffer 大小
  - 播放线程启动点

## 9. 建议阅读顺序

1. [`README.md`](/home/aitopia/Engineering_Projects/.worktrees/religion-demo-20260407/20260407_宗教大模型云服务器Demo/README.md#L1)
2. [`部署计划大纲.md`](/home/aitopia/Engineering_Projects/.worktrees/religion-demo-20260407/20260407_宗教大模型云服务器Demo/部署计划大纲.md#L1)
3. [`快速启动手册.md`](/home/aitopia/Engineering_Projects/.worktrees/religion-demo-20260407/20260407_宗教大模型云服务器Demo/快速启动手册.md#L1)
4. [`esp_idf_demo/README.md`](/home/aitopia/Engineering_Projects/.worktrees/religion-demo-20260407/20260407_宗教大模型云服务器Demo/esp_idf_demo/README.md#L1)
5. [`2026-04-08-hardware-handoff.md`](/home/aitopia/Engineering_Projects/.worktrees/religion-demo-20260407/20260407_宗教大模型云服务器Demo/docs/superpowers/summaries/2026-04-08-hardware-handoff.md#L1)
